# homebrew-ip2cr

Homebrew configurations for [IP-2-CloudResource](https://github.com/magneticstain/ip-2-cloudresource).

## Install

With homebrew installed, run the following to install IP2CR:

```bash
brew tap magneticstain/ip2cr
brew install ip2cr
```
